import { CcIfDirective } from './cc-if.directive';

describe('CcIfDirective', () => {
  it('should create an instance', () => {
    const directive = new CcIfDirective();
    expect(directive).toBeTruthy();
  });
});
