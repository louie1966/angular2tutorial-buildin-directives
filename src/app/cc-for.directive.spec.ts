import { CcForDirective } from './cc-for.directive';

describe('CcForDirective', () => {
  it('should create an instance', () => {
    const directive = new CcForDirective();
    expect(directive).toBeTruthy();
  });
});
